# @mikaelcedergren/cx-framework comes from GitHub and declares Angular 22 peers. This repo is
# itself Angular 22 (SSG) and lists its own Angular packages in package.json, so don't auto-install
# a second Angular copy for the peer, and don't fail the install over minor version skew.
auto-install-peers=false
strict-peer-dependencies=false
