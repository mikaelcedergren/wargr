# @mikaelcedergren/cx-framework is included here but NOT yet used — it's staged for a later
# rebuild. It declares Angular 22 peers; this is a plain static site with no Angular, so don't
# pull Angular into the tree for the peer, and don't fail the install over the (intentional) skew.
auto-install-peers=false
strict-peer-dependencies=false
