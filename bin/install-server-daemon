#!/usr/bin/env bash
set -euo pipefail

readonly repo="$(cd "$(dirname "$0")/.." && pwd)"
readonly installer="$repo/../server-ops/bin/install-site-service-definitions.mjs"

exec /opt/homebrew/bin/node "$installer" --site wargr --repo "$repo" "$@"
